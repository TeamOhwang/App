<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" >

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Hackathon"
        android:usesCleartextTraffic="true" >
        <activity
            android:name="com.example.hackathon.LogoutActivity"
            android:exported="false" />
        <activity
            android:name="com.example.hackathon.MainActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="false" /> <!-- android:name="com.example.hackathon.LoginActivity" -->
        <!-- android:name="com.example.project.MainActivity" -->
        <activity
            android:name="com.example.hackathon.LoginActivity"
            android:exported="true" >

            <!-- 앱 시작점 -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LiveTalkActivity"
            android:exported="false" />
    </application>

</manifest>